{
  "name": "hello-dolly",
  "scripts": {
    "test:lint": "composer run lint",
    "test:playwright": "wp-scripts test-playwright",
    "test:playwright:debug": "wp-scripts test-playwright --debug",
    "test": "npm run test:lint && npm run test:playwright",
    "wp-env": "wp-env",
    "prepare": "husky"
  },
  "devDependencies": {
    "@wordpress/scripts": "^28.3.0",
    "husky": "^9.1.0"
  }
}
